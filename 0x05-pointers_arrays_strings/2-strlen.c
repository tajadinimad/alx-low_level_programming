#include "main.h"
#include <string.h>
/**
 * _strlen - This report, by its very length,
 * defends itself
 *  against the risk of being read
 *
 * @s: pointer integer
 */
int _strlen(char *s)
{
	int c = strlen(s);

	return (c);
}
